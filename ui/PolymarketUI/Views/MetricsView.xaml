<UserControl x:Class="PolymarketUI.Views.MetricsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:PolymarketUI.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="200" d:DesignWidth="300">
    <UserControl.Resources>
        <conv:InverseBoolToColorConverter x:Key="DegradedColor"/>
    </UserControl.Resources>
    <Border BorderBrush="Gray" BorderThickness="1" Margin="4" Padding="6">
        <StackPanel>
            <TextBlock Text="Engine Metrics" FontWeight="Bold" FontSize="14" Margin="0,0,0,6"/>

            <TextBlock Text="{Binding UtcClock}" FontFamily="Consolas" FontSize="13"
                       FontWeight="Bold" Margin="4,0,0,6"/>

            <Grid Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="22"/>
                    <RowDefinition Height="22"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="REST Requests:" Style="{StaticResource MetricLabel}"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding RestRequests}" Style="{StaticResource MetricLabel}"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="REST Errors:" Style="{StaticResource MetricLabel}"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding RestErrors}" Style="{StaticResource MetricLabel}" Foreground="Red"/>
            </Grid>

            <Separator Margin="0,2"/>

            <Grid Margin="0,4,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="22"/>
                    <RowDefinition Height="22"/>
                    <RowDefinition Height="22"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Gateway:" Style="{StaticResource MetricLabel}"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Style="{StaticResource MetricLabel}"
                           Foreground="{Binding GatewayDegraded, Converter={StaticResource DegradedColor}}">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0}">
                            <Binding Path="GatewayDegraded" StringFormat="{}Degraded: {0}"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="HB OK:" Style="{StaticResource MetricLabel}"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding HeartbeatOk}" Style="{StaticResource MetricLabel}"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="HB Fail:" Style="{StaticResource MetricLabel}"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding HeartbeatFail}" Style="{StaticResource MetricLabel}" Foreground="Red"/>
            </Grid>

            <Separator Margin="0,2"/>

            <Grid Margin="0,4,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="22"/>
                    <RowDefinition Height="22"/>
                    <RowDefinition Height="22"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Risk Checks:" Style="{StaticResource MetricLabel}"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding RiskChecks}" Style="{StaticResource MetricLabel}"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Risk Allows:" Style="{StaticResource MetricLabel}"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding RiskAllows}" Style="{StaticResource MetricLabel}"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Risk Denies:" Style="{StaticResource MetricLabel}"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding RiskDenies}" Style="{StaticResource MetricLabel}" Foreground="Red"/>
            </Grid>

            <Separator Margin="0,2"/>

            <TextBlock Text="Latency (avg / p95)" FontWeight="Bold" FontSize="12" Margin="0,4,0,4"/>
            <Grid Margin="0,0,0,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="22"/>
                    <RowDefinition Height="22"/>
                    <RowDefinition Height="22"/>
                    <RowDefinition Height="22"/>
                    <RowDefinition Height="22"/>
                    <RowDefinition Height="22"/>
                    <RowDefinition Height="22"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Order RTT:" Style="{StaticResource MetricLabel}"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding OrderRttDisplay}" Style="{StaticResource MetricLabel}"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Cancel RTT:" Style="{StaticResource MetricLabel}"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding CancelRttDisplay}" Style="{StaticResource MetricLabel}"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Engine:" Style="{StaticResource MetricLabel}"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding EngineSpeedDisplay}" Style="{StaticResource MetricLabel}"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="WS Book:" Style="{StaticResource MetricLabel}"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding WsBookDisplay}" Style="{StaticResource MetricLabel}"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Binance MD:" Style="{StaticResource MetricLabel}"/>
                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding BinanceMdDisplay}" Style="{StaticResource MetricLabel}"/>

                <TextBlock Grid.Row="5" Grid.Column="0" Text="Exch Recv:" Style="{StaticResource MetricLabel}"/>
                <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding ExchRecvDisplay}" Style="{StaticResource MetricLabel}"/>

                <TextBlock Grid.Row="6" Grid.Column="0" Text="Probe:" Style="{StaticResource MetricLabel}"/>
                <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding ProbeStatusDisplay}" Style="{StaticResource MetricLabel}"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
