<UserControl x:Class="PolymarketUI.Views.WorkingOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:PolymarketUI.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="200" d:DesignWidth="600">
    <UserControl.Resources>
        <conv:OrderLifecycleToBrushConverter x:Key="OrderLifecycleBrush"/>
    </UserControl.Resources>
    <Border BorderBrush="Gray" BorderThickness="1" Margin="4" Padding="4">
        <StackPanel>
            <TextBlock Text="Working Orders" FontWeight="Bold" FontSize="14" Margin="0,0,0,4"/>
            <DataGrid ItemsSource="{Binding WorkingOrders}" Height="180" FontSize="13" ColumnWidth="*"
                      PreviewMouseWheel="DataGrid_PreviewMouseWheel">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="{Binding LifecycleState, Converter={StaticResource OrderLifecycleBrush}}"/>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding TimeStr}" Width="*"/>
                    <DataGridTextColumn Header="Exchange ID" Binding="{Binding ExchangeOrderId}" Width="*"/>
                    <DataGridTextColumn Header="Market" Binding="{Binding MarketLabel}" Width="*"/>
                    <DataGridTextColumn Header="Side" Binding="{Binding Side}" Width="*"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding PriceStr}" Width="*"/>
                    <DataGridTextColumn Header="Size" Binding="{Binding OriginalSizeStr}" Width="*"/>
                    <DataGridTextColumn Header="Filled" Binding="{Binding FilledSizeStr}" Width="*"/>
                    <DataGridTextColumn Header="Remain" Binding="{Binding RemainingStr}" Width="*"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock Text="Closed Orders" FontWeight="Bold" FontSize="14" Margin="0,8,0,4"/>
            <DataGrid ItemsSource="{Binding ClosedOrders}" Height="180" FontSize="13" ColumnWidth="*"
                      PreviewMouseWheel="DataGrid_PreviewMouseWheel">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="{Binding LifecycleState, Converter={StaticResource OrderLifecycleBrush}}"/>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding TimeStr}" Width="*"/>
                    <DataGridTextColumn Header="Exchange ID" Binding="{Binding ExchangeOrderId}" Width="*"/>
                    <DataGridTextColumn Header="Market" Binding="{Binding MarketLabel}" Width="*"/>
                    <DataGridTextColumn Header="Side" Binding="{Binding Side}" Width="*"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding PriceStr}" Width="*"/>
                    <DataGridTextColumn Header="Size" Binding="{Binding OriginalSizeStr}" Width="*"/>
                    <DataGridTextColumn Header="Filled" Binding="{Binding FilledSizeStr}" Width="*"/>
                    <DataGridTextColumn Header="Remain" Binding="{Binding RemainingStr}" Width="*"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock Text="Trade History" FontWeight="Bold" FontSize="14" Margin="0,8,0,4"/>
            <DataGrid ItemsSource="{Binding TradeHistory}" Height="180" FontSize="13" ColumnWidth="*"
                      PreviewMouseWheel="DataGrid_PreviewMouseWheel">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="{Binding Status, Converter={StaticResource OrderLifecycleBrush}}"/>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding TimeStr}" Width="*"/>
                    <DataGridTextColumn Header="Trade ID" Binding="{Binding TradeId}" Width="*"/>
                    <DataGridTextColumn Header="Market" Binding="{Binding MarketLabel}" Width="*"/>
                    <DataGridTextColumn Header="Side" Binding="{Binding Side}" Width="*"/>
                    <DataGridTextColumn Header="Buy/Sell" Binding="{Binding BuySell}" Width="*"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding PriceStr}" Width="*"/>
                    <DataGridTextColumn Header="Size" Binding="{Binding SizeStr}" Width="*"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Border>
</UserControl>
