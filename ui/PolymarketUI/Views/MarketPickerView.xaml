<UserControl x:Class="PolymarketUI.Views.MarketPickerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <GroupBox Header="BTC Ladder Watch" Margin="4" Padding="4">
        <ItemsControl ItemsSource="{Binding Series}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="4,2">
                        <TextBlock Text="{Binding DisplayName}" Margin="0,0,8,0"
                                   FontFamily="Consolas" FontSize="12" FontWeight="Bold"
                                   VerticalAlignment="Center"/>
                        <Button Content="{Binding ToggleButtonText}" Command="{Binding ToggleLadderCommand}"
                                Width="50" Height="22" FontSize="11" FontWeight="Bold"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </GroupBox>
</UserControl>
