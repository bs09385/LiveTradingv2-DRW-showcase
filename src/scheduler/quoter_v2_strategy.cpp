// QuoterV2Strategy — boilerplate template.
//
// The full strategy implementation has been removed from this snapshot.
// All public methods are inlined in the header as trivial stubs so that
// the scheduler, config loader, and main.cpp wiring still compile.
//
// See `quoter_v2_strategy.h` for the surface, and `strategy.h` for the
// Strategy interface contract.

#include "scheduler/quoter_v2_strategy.h"
